#include "GomokuCore.h"
#include <iostream>
// #include <type_traits>

int main()
{
  // GomokuCore::Piece piece = GomokuCore::Piece::BLACK;
  GomokuCore gomoku;
  // for (int i = 0; i < gomoku.board.size(); i++)
  // {
  //   for (int j = 0; j < gomoku.board[i].size(); j++)
  //   {
  //     std::cout << gomoku.board[i][j] << " ";
  //   }
  //   std::cout << "\n";
  // }
  return 0;
}
